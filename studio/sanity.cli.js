/**
 * Configuración del CLI de Sanity (comandos dev, build, deploy).
 * projectId y dataset deben coincidir con tu .env del sitio Astro.
 */
export default {
  project: { projectId: 'fcrjghqs' },
  dataset: 'production',
};
